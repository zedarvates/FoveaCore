[package]
name = "foveacore"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Utilisation de la branche master de gdext pour un support optimal de Godot 4.x
godot = { git = "https://github.com/godot-rust/gdext", branch = "master" }
