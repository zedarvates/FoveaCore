# WorldMirror 2.0 dependencies for FoveaEngine
# Install with: pip install -r requirements_worldmirror.txt
# Requires CUDA 12.4+ or CPU fallback (slow)

torch==2.4.0
torchvision==0.19.0

# Install from official HY-World-2.0 repo:
# git clone https://github.com/Tencent-Hunyuan/HY-World-2.0
# cd HY-World-2.0 && pip install -r requirements.txt
# Then ensure hyworld2/ is in PYTHONPATH or installed via pip install -e .

# Optional: FlashAttention for faster inference
# pip install flash-attn --no-build-isolation
